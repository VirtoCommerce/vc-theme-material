<div class="central">
  <div id="col-main" class="clearfix">  
    <div id="customer-activate">
      <h2 class="page-title">{{ 'customer.activate_account.title' | t }}</h2>

      <p>{{ 'customer.activate_account.subtext' | t }}</p>

        <form accept-charset="UTF-8" action="{{ '~/account/activate' | absolute_url }}" method="post" id="activate_customer_password" name="activate_customer_password">

        {% include 'form-errors-custom' %}

        <div class="group-input">      
        <input type="password" required="required" name="customer[password]" id="customer_password" />
        <span class="highlight"></span>
        <span class="bar"></span>
        <label>{{ 'customer.activate_account.password' | t }} *</label>
      </div>
      
      <div class="group-input">      
        <input type="password" required="required" name="customer[password_confirmation]" id="customer_password_confirmation" />
        <span class="highlight"></span>
        <span class="bar"></span>
        <label>{{ 'customer.activate_account.password_confirm' | t }} *</label>
      </div>
      
      <div class="control-group last">
        <button class="btn btn-1 ripple" type="submit">{{ 'customer.activate_account.submit' | t }}</button>
        <button type="submit" class="btn cancel" name="decline" id="customer_decline">{{ 'customer.activate_account.cancel' | t }}</button>
      </div>
      {{ antiforgery }}
      </form>
    </div>
  </div>
</div>
